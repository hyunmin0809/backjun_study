#include <iostream>
#include <vector>

using namespace std;

int solution(   vector<int> &record){
    int cnt = 0;
    
    return cnt;
}

main(){
    int n;
    cin >> n;

    vector<int> record(n, 0);

    for(int i = 0 ; i < n; i++){
        cin >> record[i];
    }

    int ans = solution(record)

    return 0;
}